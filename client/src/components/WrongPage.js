import React from 'react';
import Header from './Header';

const WrongPage = () => (
  <div>
    <Header />
    <h1> Not Found </h1>
    <h4> The page you are looking for isn't here. </h4>
  </div>
);

export default WrongPage;
